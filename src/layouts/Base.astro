---
import '../styles/global.css';
import { person } from '../data/person';

type Props = {
  title: string;
  description?: string;
  pageId?: string;
  ogType?: 'website' | 'article';
};

const { title, description = person.description, pageId = 'index', ogType = 'website' } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
const ogImage = new URL('/og.png', Astro.site);

const nav = [
  { href: '/', id: 'index', label: '_index' },
  { href: '/bio', id: 'bio', label: '_bio' },
  { href: '/work', id: 'work', label: '_work' },
  { href: '/writing', id: 'writing', label: '_writing' },
];

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'ProfilePage',
  mainEntity: {
    '@type': 'Person',
    '@id': `${person.site}/#person`,
    name: person.name,
    alternateName: [person.handle, '0xzick', 'zick0x'],
    url: person.site,
    email: `mailto:${person.email}`,
    jobTitle: 'Engineering Lead / Protocol Engineer',
    homeLocation: { '@type': 'Place', name: 'Dubai, United Arab Emirates' },
    knowsLanguage: ['en', 'ru'],
    alumniOf: {
      '@type': 'CollegeOrUniversity',
      name: 'Vladivostok State University of Economics and Service',
    },
    sameAs: [person.github, person.x, person.telegram],
    description: person.description,
    founder: [
      { '@type': 'Organization', name: 'NFT Multisender', url: 'https://nft.multisender.app' },
      { '@type': 'Organization', name: 'zRexFinance', url: 'https://github.com/zrex-finance/core-contracts' },
      { '@type': 'Organization', name: 'NFTxCards', url: 'https://nftxcards.com' },
    ],
  },
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />
    <meta name="theme-color" content="#0b1118" media="(prefers-color-scheme: dark)" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content={person.handle} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@0xZick" />
    <meta name="twitter:creator" content="@0xZick" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate" type="application/rss+xml" title={`${person.name} — writing`} href="/rss.xml" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,400;0,500;0,700;1,400&display=swap"
      rel="stylesheet"
    />
    <script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </head>
  <body class={pageId === 'index' ? 'page-index' : undefined}>
    <div class="layout">
      <header class="header">
        <div class="header-inner">
          <a class="logo" href="/" aria-label={person.handle}>
            <span class="logo-text">_{person.handle.toLowerCase()}</span>
          </a>

          <nav class="nav" aria-label="Site">
            <ul class="nav-list">
              {
                nav.map((item) => (
                  <li>
                    <a href={item.href} aria-current={pageId === item.id ? 'page' : undefined}>
                      {item.label}
                    </a>
                  </li>
                ))
              }
            </ul>
          </nav>

          <div class="header-actions">
            <a class="btn btn-dark" href={person.telegram}>_telegram</a>
            <button class="burger" type="button" aria-label="Open menu" data-burger>
              <span></span>
            </button>
          </div>
        </div>
        <nav class="mobile-nav" data-mobile-nav>
          {
            nav.map((item) => (
              <a href={item.href} aria-current={pageId === item.id ? 'page' : undefined}>
                {item.label}
              </a>
            ))
          }
          <a href={person.telegram}>_telegram</a>
        </nav>
      </header>

      <main class="main">
        <slot />
      </main>

      <footer class="footer">
        <div class="footer-inner">
          <span>© {new Date().getFullYear()} {person.name} · {person.location}</span>
          <div class="footer-socials">
            <span>_socials:</span>
            <a href={person.github}>github</a>
            <a href={person.x}>x.com</a>
            <a href={person.telegram}>telegram</a>
            <a href={`mailto:${person.email}`}>email</a>
          </div>
          <span>
            <a href="/llms.txt">_llms.txt</a> · <a href="/rss.xml">_rss</a> · <a href="/Nikita-Dementev-CV.pdf">_cv.pdf</a>
          </span>
        </div>
      </footer>
    </div>
    <script>
      const burger = document.querySelector('[data-burger]');
      const panel = document.querySelector('[data-mobile-nav]');
      burger?.addEventListener('click', () => {
        panel?.classList.toggle('is-open');
      });
    </script>
  </body>
</html>
